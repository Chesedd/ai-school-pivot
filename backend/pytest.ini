[pytest]
pythonpath = .
asyncio_mode = strict
markers =
    asyncio: asynchronous test
    integration: requires the dedicated PostgreSQL test database
    student_race: controlled PostgreSQL student lifecycle race
