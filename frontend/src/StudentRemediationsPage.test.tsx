import {afterEach,expect,it,vi} from "vitest";import {cleanup,render,screen} from "@testing-library/react";import {StudentRemediationsPage} from "./StudentRemediationsPage";import {listStudentRemediations} from "./remediationApi";
vi.mock("./remediationApi",()=>({listStudentRemediations:vi.fn()}));afterEach(()=>{cleanup();vi.clearAllMocks()});
it("renders only API-delivered assigned/cancelled plans and no draft UI",async()=>{vi.mocked(listStudentRemediations).mockResolvedValue({items:[{id:"a",title:"Назначенная",assigned_at:null,due_at:null,item_count:2,status:"assigned"},{id:"c",title:"Отменённая",assigned_at:null,due_at:null,item_count:1,status:"cancelled"}]});render(<StudentRemediationsPage navigate={vi.fn()}/>);expect(await screen.findByText("Назначенная")).toBeTruthy();expect(screen.getByText("Назначена")).toBeTruthy();expect(screen.getByText("Отменена")).toBeTruthy();expect(screen.queryByText(/черновик/i)).toBeNull()});
