import {useState,type FormEvent} from "react";import {ApiError} from "./api";import {useAuth} from "./auth";import {errorMessage} from "./errors";
export function LoginPage(){const {login}=useAuth(),[name,setName]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError("");try{await login(name,password);setPassword("")}catch(x){setPassword("");setError(x instanceof ApiError&&x.code==="invalid_credentials"?"Неверный логин или пароль":errorMessage(x,"Не удалось войти. Проверьте соединение с сервером."))}finally{setBusy(false)}};return <main className="auth-main"><section className="auth-card"><h1>Вход</h1><form onSubmit={submit}><label htmlFor="login">Логин</label><input id="login" autoComplete="username" required value={name} onChange={e=>setName(e.target.value)}/><label htmlFor="password">Пароль</label><input id="password" type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div role="alert" className="alert error">{error}</div>}<button disabled={busy}>{busy?"Вход…":"Войти"}</button></form></section></main>}
