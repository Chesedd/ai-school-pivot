import {afterEach,expect,it,vi} from "vitest";
import {checkTaskDuplicates} from "./api";
afterEach(()=>vi.restoreAllMocks());
it("posts the exact duplicate-check payload and signal",async()=>{const response={has_likely_duplicates:false,items:[]};const fetch=vi.spyOn(globalThis,"fetch").mockImplementation(()=>Promise.resolve(new Response(JSON.stringify(response),{status:200,headers:{"Content-Type":"application/json"}})));const payload={statement:"Find x",primary_skill_id:"skill",final_answer:null,exclude_task_id:null,limit:5};const signal=new AbortController().signal;await expect(checkTaskDuplicates(payload,signal)).resolves.toEqual(response);expect(fetch.mock.calls[0]).toEqual(["http://localhost:8000/api/content-bank/task-versions/check-duplicates",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload),signal,credentials:"include"}])});
