import {describe,expect,it} from "vitest";import {normalizeRows} from "./importCatalog";import type {Catalogs} from "./main";import {IMPORT_HEADERS} from "./importTypes";
const catalogs:Catalogs={subjects:[{id:"s",name:"Math",code:"math"}],grades:[{id:"g",name:"7",number:7}],topics:[{id:"t",name:"Motion",code:"motion",subject_id:"s",grade_id:"g"}],subtopics:[{id:"st",name:"Speed",code:"speed",topic_id:"t"}],skills:[{id:"sk",name:"Solve",code:"solve",topic_id:"t",subtopic_id:"st"}]};
const row=(change:Record<string,string>={})=>({row_number:9,issues:[],values:{...Object.fromEntries(IMPORT_HEADERS.map(x=>[x,""])),subject_code:"math",grade_number:"7",topic_code:"motion",subtopic_code:"speed",statement:"Do",task_type:"problem",answer_format:"number",difficulty:"25",primary_skill_code:"solve",primary_skill_weight:"1",...change}} as any);
describe("catalog normalization",()=>{it("resolves codes to UUIDs and preserves row",()=>{const x=normalizeRows([row()],catalogs);expect(x.payload[0]).toMatchObject({row_number:9,subject_id:"s",grade_id:"g",topic_id:"t",subtopic_id:"st"})});it("excludes unknown codes",()=>{const x=normalizeRows([row({subject_code:"unknown"})],catalogs);expect(x.payload).toEqual([]);expect(x.rows[0].issues[0].code).toBe("catalog_reference_not_found")});it("rejects hierarchy mismatch",()=>{const x=normalizeRows([row()],{...catalogs,topics:[{...catalogs.topics[0],subject_id:"other"}]});expect(x.payload).toEqual([]);expect(x.rows[0].issues.some(i=>i.code==="catalog_hierarchy_mismatch")).toBe(true)})});
